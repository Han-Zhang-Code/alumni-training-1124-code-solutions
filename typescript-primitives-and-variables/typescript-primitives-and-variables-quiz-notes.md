# typescript-primitives-and-variables-quiz-notes

## Quiz Questions

Answer the following questions in the provided markdown file before turning in this exercise:

- What is the purpose of variables?
  the variables can hold values to use in the code
- How do you **declare** a variable?
  use let or const or var key word
- How do you initialize (**assign a value to**) a variable?
  use =
- What characters are allowed in variable names?
  Characters can include letters, digits, underscores, and dollar signs, but must start with a letter, $, or \_.
- What does it mean to say that variable names are "case sensitive"?
  Two variables with the same name, but with different capitalization, are treated as 2 separate variables
- What is the purpose of a string?
  to represent text
- What is the purpose of a number?
  represent both integer and floating-point values
- What is the purpose of a boolean?
  typically used in conditional testing
- What does the `=` operator mean in JavaScript?
  assign
- How do you update the value of a variable?
  use `=` operator
- What is the difference between `null` and `undefined`?
  null is an object, undefined is undefined
- Why is it a good habit to include "labels" when you log values to the browser console?
  so you know which is which
- Give five examples of JavaScript primitives.
  String, Number, Boolean, Null, Undefined, Symbol

## Notes

All student notes should be written here.

How to write `Code Examples` in markdown

for JS:

```javascript
const data = 'Howdy';
```

for HTML:

```html
<div>
  <p>This is text content</p>
</div>
```

for CSS:

```css
div {
  width: 100%;
}
```
